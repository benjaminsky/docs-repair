# Working notes

## Non-negotiables
- **Every claim carries an evidence tag.** `[V]` verified against a primary
  source, `[H]` hypothesis. Never blur the two.
- Rule sets export a `*_RULE_VERSION` string stamped on every output row.
